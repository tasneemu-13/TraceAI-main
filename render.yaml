services:
  - type: web
    name: traceai-platform
    runtime: docker
    plan: free
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: traceai-mysql
          property: connectionString

databases:
  - name: traceai-mysql
    plan: free
    databaseName: traceai
    user: trace_user
